#!/bin/bash

# omarchy:summary=Update packages from enabled Fedora RPM repositories
# omarchy:requires-sudo=true

set -euo pipefail
if [[ ${OMARCHY_UPDATE_UNATTENDED:-0} == 1 ]]; then
  exec omarchy-update-dnf -y upgrade --refresh
else
  exec omarchy-update-dnf upgrade --refresh
fi
