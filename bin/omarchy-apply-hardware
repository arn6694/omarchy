#!/bin/bash
# omarchy:summary=Configure Fedora NVIDIA graphics packages when requested

set -euo pipefail
case "${1:-}" in
  --nvidia) exec bash "$OMARCHY_PATH/install/fedora/nvidia.sh" ;;
  *) echo 'Fedora manages standard hardware drivers. Use --nvidia for RPM Fusion NVIDIA packages.' ;;
esac
