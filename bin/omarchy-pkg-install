#!/bin/bash

# omarchy:summary=Choose packages from enabled Fedora RPM repositories.
# omarchy:requires-sudo=true

fzf_args=(
  --multi
  --preview 'dnf info --available {1}'
  --preview-label='alt-p: toggle description, alt-j/k: scroll, tab: multi-select'
  --preview-label-pos='bottom'
  --preview-window 'down:65%:wrap'
  --bind 'alt-p:toggle-preview'
  --bind 'alt-d:preview-half-page-down,alt-u:preview-half-page-up'
  --bind 'alt-k:preview-up,alt-j:preview-down'
  --color 'pointer:green,marker:green'
)

set -o pipefail
pkg_names=$(dnf -q repoquery --available --queryformat '%{name}\n' | sort -u | fzf "${fzf_args[@]}") || exit $?

if [[ -n $pkg_names ]]; then
  source omarchy-sudo-keepalive

  mapfile -t packages <<< "$pkg_names"
  omarchy-pkg-add "${packages[@]}"
  omarchy-show-done $?
fi
