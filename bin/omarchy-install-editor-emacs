#!/bin/bash
# omarchy:summary=Install Fedora Emacs

set -euo pipefail
omarchy-pkg-add emacs
echo 'The upstream custom Emacs configuration package is omitted; Fedora Emacs is installed.'
setsid uwsm-app -- emacs >/dev/null 2>&1 &
