#!/bin/bash

# omarchy:summary=Install the verified default Fedora RPM set
# omarchy:requires-sudo=true

set -euo pipefail
mapfile -t packages < <(grep -Ev '^(#|$)' "$OMARCHY_PATH/install/fedora/base.packages")
omarchy-pkg-add "${packages[@]}"
