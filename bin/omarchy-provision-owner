#!/bin/bash

# omarchy:summary=Report an omitted Fedora feature
# omarchy:hidden=true

printf '%s\n' "Deferred ISO owner provisioning is not included. Create the user in Fedora Server's installer." >&2
exit 69
