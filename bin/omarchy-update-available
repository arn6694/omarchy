#!/bin/bash

# omarchy:summary=Check for available Fedora RPM updates

set -euo pipefail
updates=$(dnf -q repoquery --upgrades --queryformat '%{name} %{evr}\n')
if [[ -n $updates ]]; then
  printf '%s\n' "$updates"
else
  echo "Fedora packages are up to date"
  exit 1
fi
