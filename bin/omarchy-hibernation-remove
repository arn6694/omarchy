#!/bin/bash

# omarchy:summary=Report an omitted Fedora feature
# omarchy:hidden=true

printf '%s\n' 'Automatic hibernation configuration removal is omitted; Fedora owns the swap and resume configuration.' >&2
exit 69
