#!/bin/bash
# omarchy:summary=Set up Omadora configuration for the current Fedora user
# omarchy:hidden=true

set -euo pipefail
if [[ ${1:-} != "--force" && -f $HOME/.local/state/omarchy/fedora-user-ready ]]; then
  exit 0
fi
exec bash "$OMARCHY_PATH/install/fedora/setup-user.sh" "$@"
